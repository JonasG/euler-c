#include <math.h>
#include <stdbool.h>
#include <stdint.h>

// Return true if n is prime, false otherwise
bool is_prime(uint64_t n);

// Returns the next prime after the after parameter
uint32_t next_prime(uint32_t after);
